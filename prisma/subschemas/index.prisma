import { Rules } from "../schema"

model Rules {
    id     String  @unique
    title  String
    config Boolean @default(true)
    src    String  @default("core-rules")
}

model Index {
    id    String @unique
    table String
}
